import css from 'styled-jsx/css';

export default css`
  .event-message {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    padding: 0 0 0 1.2rem;
  }
  .event-message p {
    margin: 0;
    color: var(--col-grey);
  }
`;
