import css from 'styled-jsx/css';

export default css`
  .card-container {
    border: 2px solid var(--col-info-400);
    position: relative;
    display: inline-flex;
    padding: 0;
    width: 15rem;
    height: 15rem;
  }
`;
